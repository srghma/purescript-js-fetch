{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .spago",
    "build": "eslint src && purs-tidy check --config-require src/**/*.purs && spago build --censor-stats --strict --pedantic-packages",
    "test": "spago test"
  },
  "devDependencies": {
    "eslint": "^9.12.0",
    "pulp": "^16.0.2",
    "purescript-psa": "^0.9.0",
    "purs-tidy": "^0.11.0",
    "rimraf": "^6.0.1"
  }
}
